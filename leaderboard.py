#leaderboard_run(Leaderboards.Cactus, "cactus", 10000)

# to get num_items(Items.Power) >= 100000, we need 14 runs currently. hardcode this many loops into the code before running
leaderboard_run(Leaderboards.Sunflowers, "Sunflowers", 10000)