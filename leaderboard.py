#leaderboard_run(Leaderboards.Cactus, "cactus", 10000)

# to get num_items(Items.Power) >= 100000, we need 14 runs currently
#leaderboard_run(Leaderboards.Sunflowers, "Sunflowers", 10000)

# Need to get num_items(Items.Carrot) >= 2000000000
#leaderboard_run(Leaderboards.Carrots, "polyculture_carrot", 10000)

# Need to get num_items(Items.Wood) >= 10000000000
leaderboard_run(Leaderboards.Wood, "polyculture_tree", 10000)